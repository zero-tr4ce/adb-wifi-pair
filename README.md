# adb-wifi-pair
# 📱 ADB Wireless & Scrcpy Setup (Windows Only)

![Windows](https://img.shields.io/badge/OS-Windows-blue)
![Python](https://img.shields.io/badge/Python-3.13+-yellow)

> A simple Windows tool to pair your Android device over Wi-Fi, run scrcpy, and manage ADB. Includes automatic fixes via `fix.py`.

---

## ⚠️ Windows Only

This project **only works on Windows**.  
It relies on Windows-specific executables like `adb-wifi.exe`, `scrcpy.exe`, and `pywin32`.

---

## 🛠 Requirements

- Python 3.13+  
- Windows OS  
- Installed Python dependencies:

```bash
pip install -r requirements.txt -q
